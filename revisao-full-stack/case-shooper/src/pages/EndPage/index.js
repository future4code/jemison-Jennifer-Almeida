import React from 'react';
import Header from '../../components/Header';

export default function EndPage() {
    return (
        <div>
            <Header />
            <h1>It's the End!</h1>
        </div>
    );
}